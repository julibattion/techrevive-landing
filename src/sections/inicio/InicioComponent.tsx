import React from "react";
import styles from './inicio.module.css';

export default function InicioComponent() {
    return (
        <div>
            <section className={styles.index} id="inicio">
        <div>
        </div>
            </section>
        </div>
    );
}